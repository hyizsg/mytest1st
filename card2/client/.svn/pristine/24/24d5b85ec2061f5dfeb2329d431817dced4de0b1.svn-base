//
//  HConfig.cpp
//  card
//
//  Created by zhou gang on 13-6-5.
//
//

#include "../../headers.h"
#include "HConfig.h"


vector<string> HHero::strings;
map<int, const HHero*> HHero::heros;

vector<string> HCard::strings;
map<int, const HCard*> HCard::cards;

vector<string> HLevel::strings;
map<int, const HLevel*> HLevel::levels;

vector<string> HMagic::strings;
map<int, vector<const HMagic*> > HMagic::magics;

vector<string> HSkill::strings;
map<int, const HSkill*> HSkill::skills;

vector<string> HBuff::strings;
map<int, const HBuff*> HBuff::buffs;

vector<string> HProp::strings;
map<int, const HProp*> HProp::props;

vector<string> HStrong::strings;
map<int, const HStrong*> HStrong::strongs;

vector<string> HExchange::strings;
map<int, const HExchange*> HExchange::exchanges;
vector<const HExchange*> HExchange::exchangelist;


vector<string> HTemple::strings;
map<int, const HTemple*> HTemple::temples;

vector<string> HHeaven::strings;
map<int, const HHeaven*> HHeaven::heavens;
map<int, vector<const HHeaven*> > HHeaven::heavensList;


vector<string> HSymbol::strings;
map<int, vector<const HSymbol*> > HSymbol::symbols;


vector<string> HPvp::strings;
map<int, const HPvp*> HPvp::pvps;

vector<string> HFcPvp::strings;
map<int, const HFcPvp*> HFcPvp::pvps;

vector<string> HUpgrade::strings;
map<int, const HUpgrade*> HUpgrade::grades;


vector<string> HUgExchange::strings;
map<int, vector<const HUgExchange*> > HUgExchange::exchanges;


vector<string> HMonster::strings;
map<int, const HMonster*> HMonster::monsters;

vector<string> HChallenge::strings;
map<int, vector<const HChallenge*> > HChallenge::challenges;

vector<string> HNewbie::strings;
map<int, vector<const HNewbie* > > HNewbie::newbies;


vector<string> HAchieve::strings;
map<int, const HAchieve*> HAchieve::achieves;
vector<const HAchieve*> HAchieve::achievelist;
